package com.gamification.streaks.enums;

public enum RuleType {
    XP,
    BADGE,
    STREAK,
    LEADERBOARD,
    REWARD,
    XP_ENGAGEMENT,
    XP_PERFORMANCE,
    XP_SPEND,
    STREAK_RULE,
    COIN_CONVERSION,
    COIN_REDEMPTION
}
