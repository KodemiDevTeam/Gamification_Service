spring:
  application:
    name: streaks
server:
  port: 8087

aws:
  accessKey: g2r1e4
  secretKey: s9sa6n
  region: us-east-1

dynamodb:
  endpoint: http://localhost:8000

jwt:
  secret: test-secret-key-that-is-long-enough-for-hmac-sha256